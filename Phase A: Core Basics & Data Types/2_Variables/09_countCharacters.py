# Goal: Use variables to store and compute lengths.
# Expected output (exactly 1 line):
# length=6

word = "python"
length= len(word)

# TODO: store the length of word in a variable called length

print("length=" + str(length))
